import React, { Component } from 'react'


//Actions symbol -> ()  
//data symbol -> #


//set (add) #tweets data#
// (get) #authed user# so the user can create new tweet

//this component will be CONTAINER so it can dispatch an action from the STORE to let the reducers know that a new tweet has been made ,,, and also can read state from the store. --so we will use connect() method here

class NewTweet extends Component {
  render() {
    return (
      <div>
        {//form
        //button
        }
      </div>
    )
  }
}

export default NewTweet